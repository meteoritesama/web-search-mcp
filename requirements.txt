# 必需
httpx>=0.27.0
beautifulsoup4>=4.12.0
# 可选:更健壮的 HTML 解析;不装会自动回退到标准库 html.parser
lxml>=5.2.0
# 网页抓取(整页解析/图片提取)。安装后还需: playwright install chromium
crawl4ai>=0.5.0
# 可选(路线 A · F5):视觉模型图片降采样,减少图片 token、提速省显存;不装自动跳过降采样
Pillow>=10.0.0
